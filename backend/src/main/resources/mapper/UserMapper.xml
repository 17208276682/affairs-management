<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.affairs.management.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.affairs.management.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="deptId" column="dept_id"/>
        <result property="position" column="position"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="UserResultMap">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <select id="selectByDeptId" resultMap="UserResultMap">
        SELECT * FROM users WHERE dept_id = #{deptId} AND status = 1
        ORDER BY created_at
    </select>

    <select id="selectByDeptIds" resultMap="UserResultMap">
        SELECT * FROM users WHERE status = 1
        AND dept_id IN
        <foreach collection="deptIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY dept_id, created_at
    </select>

    <select id="selectMemberList" resultMap="UserResultMap">
        SELECT * FROM users
        <where>
            <if test="deptId != null and deptId != ''">
                AND dept_id = #{deptId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                     OR username LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="position != null and position != ''">
                AND position LIKE CONCAT('%', #{position}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countMembers" resultType="long">
        SELECT COUNT(*) FROM users
        <where>
            <if test="deptId != null and deptId != ''">
                AND dept_id = #{deptId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                     OR username LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="position != null and position != ''">
                AND position LIKE CONCAT('%', #{position}, '%')
            </if>
        </where>
    </select>

    <insert id="insert">
        INSERT INTO users (id, username, password_hash, name, avatar, phone, email,
                           dept_id, position, role, status)
        VALUES (#{id}, #{username}, #{passwordHash}, #{name}, #{avatar}, #{phone}, #{email},
                #{deptId}, #{position}, #{role}, #{status})
    </insert>

    <update id="update">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="passwordHash != null">password_hash = #{passwordHash},</if>
            <if test="name != null">name = #{name},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="position != null">position = #{position},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="selectManagerByDeptId" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE dept_id = #{deptId} AND role = 'manager' AND status = 1
        <if test="excludeId != null and excludeId != ''">
            AND id != #{excludeId}
        </if>
        LIMIT 1
    </select>

    <select id="selectMaxId" resultType="string">
        SELECT MAX(id) FROM users
    </select>

    <select id="selectByIds" resultMap="UserResultMap">
        SELECT * FROM users WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="countTasksByUserId" resultType="int">
        SELECT COUNT(*) FROM tasks
        WHERE assigner_id = #{userId} OR executor_id = #{userId}
    </select>
</mapper>
